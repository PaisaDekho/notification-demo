import Image from 'next/image'

export default function Home() {
    return (
        <main className={"w-full min-h-screen flex flex-col items-center justify-center"}>
            <h2 className={"text-2xl font-semibold"}>Notification DEMO</h2>
        </main>
    )
}
